# EcoLeak AI - P3 (Carbon Accounting & Simulation Engine)
# Core engine is pure deterministic Python. scikit-learn is used ONLY by Module H.
pydantic>=2.0
scikit-learn>=1.3
numpy>=1.24
# SQL-backed data source (Phase 2: reads P2's tables; SQLite image in tests)
sqlalchemy>=2.0
# Optional service wrapper (the engine also works as a plain importable library)
fastapi>=0.110
uvicorn[standard]>=0.27
# Test / validation only
pytest>=8.0
httpx>=0.27
hypothesis>=6.0
